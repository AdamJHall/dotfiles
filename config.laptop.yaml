imports:
  - config.base.yaml

system:
  hostname: adam-laptop

disks:
  layout: lvm
  esp:
    device: /dev/nvme0n1
    size: 4GiB
  swap:
    type: swapfile
    size: 64GiB
  lvm:
    vg: vg0
    lv: root
    filesystem: xfs
    pvs:
      - /dev/nvme0n1p2
      - /dev/nvme1n1

packages:
  - cachyos-v3/nvidia-utils
  - cachyos-v3/lib32-nvidia-utils

hooks:
  - name: Install Intel drivers
    at: after:packages
    root: true
    run: chwd --install intel

  - name: Install Nvidia drivers
    at: after:packages
    root: true
    run: chwd --install nvidia-open-dkms.prime